first page
plan
why git?
  plan
  branching and merging
  distributed VCS
  small and fast
  history
  staging area
  misc
minusus
terminology
  work space, staging area=index, repository
  log --graph --all --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(bold white)— %an%C(reset)%C(bold yellow)%d%C(reset)' --abbrev-commit --date=relative
  objects (blob, tree, commit, tag)
  references: branches, HEAD, tags, remotes, stash
  specifying revisions ~, ^ https://www.kernel.org/pub/software/scm/git/docs/gitrevisions.html
  ignore files
best practices
how to work with
commands
  full list
  essential
  config
  init clone
hands-on
resources
  guis
  links
last page
